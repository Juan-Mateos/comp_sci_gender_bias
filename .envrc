#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "comp_sci_gender_bias")"
[ "$CONDA_DEFAULT_ENV" = "comp_sci_gender_bias" ] || echo "Have you run \`make install\`?"
